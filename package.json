  {
    "name": "flower-biller",
    "version": "1.0.0",
    "scripts": {
        "heroku-prebuild": "cd client && npm install && npm run build && cd .. && cd server && npm install",
        "start": "cd server && npm run start"
      },
    "engines": {
      "node": "18.x"
    }
  }